import CardForms, { type CardFormProps } from "~/components/card-forms";

export default function Notifications() {
  return (
      <CardForms title="notification" des="da">
          s
      </CardForms>
  )

}